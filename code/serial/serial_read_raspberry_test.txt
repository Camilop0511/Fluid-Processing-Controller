$serialPort = fopen('/dev/ttyS0', 'r');
system('stty -F /dev/ttyS0 57600');
while (true) {
    $byte = fread($serialPort, 1);
    if ($byte === ) {
        // do something when the byte 0xC8 is read
        echo "0xC8 is read\n";
    }
}

fclose($serialPort);