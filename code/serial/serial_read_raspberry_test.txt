$serialPort = fopen('/dev/ttyS0', 'r');
system('stty -F /dev/ttyS0 57600');

while (true) {
    $byte1 = fread($serialPort, 1);
    $byte2 = fread($serialPort, 1);
    if ($byte1 === "\xa3") {
        echo "Step: " . ord($byte2) . "\n";
    }
}

fclose($serialPort);